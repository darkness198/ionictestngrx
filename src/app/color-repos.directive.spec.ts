import { ColorReposDirective } from './color-repos.directive';

describe('ColorReposDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorReposDirective();
    expect(directive).toBeTruthy();
  });
});
